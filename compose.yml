services:
  walloftext:
    restart: unless-stopped
    container_name: walloftext
    image: walloftext
    pull_policy: never
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/app/data
    env_file:
      - .env
